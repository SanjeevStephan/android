<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/navigation"
    app:startDestination="@id/home">

    <fragment
        android:id="@+id/home"
        android:name="com.sanjeev.stephan.murmu.smarthome.Home"
        android:label="fragment_home"
        tools:layout="@layout/fragment_home" >
        <action
            android:id="@+id/action_home_to_sampleDevice"
            app:destination="@id/sampleDevice" />
    </fragment>
    <fragment
        android:id="@+id/sampleDevice"
        android:name="com.sanjeev.stephan.murmu.smarthome.sample.SampleDevice"
        android:label="fragment_sample_device"
        tools:layout="@layout/fragment_sample_device" />
</navigation>